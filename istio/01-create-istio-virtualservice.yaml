kind:       VirtualService
apiVersion: networking.istio.io/v1alpha3
metadata:
  name:      helloworld
  namespace: helloworld
spec:
  hosts:
  - "*"
  gateways:
  - helloworld-gateway
  http:
  - match:
    - uri:
        exact: /
    route:
    - destination:
        host: helloworld
